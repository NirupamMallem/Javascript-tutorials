let txt = document.getElementById('text')
txt.addEventListener('keydown',anykey)

function anykey(ev)
{
    let target = ev.currentTraget
    let tag = ev.tagName

}

// keypress- keydown+keyup

